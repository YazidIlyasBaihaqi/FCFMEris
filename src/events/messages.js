/*const Queue = require("../database/models/guildSchema")

module.exports = async function message(){
    bot.on("messageCreate", (msg) => {
        let database_queue = await Queue.findOne({
            guildID: msg.guildId,
          });
        if (database_queue.prefix == )
    })
}*/